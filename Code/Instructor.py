class Instructor:
    def __init__(self, instructor_id, name, salary_level, min_class, max_class, ideal_class):
        self.id = instructor_id
        self.name = name
        self.salary_level = salary_level
        self.min_class = min_class
        self.max_class = max_class
        self.ideal_class = ideal_class